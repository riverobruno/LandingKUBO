# Apply Progress: Sonar Quality Hardening

## Scope

- Change: `sonar-quality-hardening`
- Slice: SHA-only SAST remediation
- Delivery: single configuration change
- Review boundary: six Docker action reference substitutions in `.github/workflows/ci.yml`
- Mode: Structural validation; no application test cycle applies

## Completed Tasks

- [x] 1.1 Enumerate the six Docker action occurrences and intended full SHAs.
- [x] 1.2 Validate the four selected full SHA values.
- [x] 1.3 Substitute only the six mutable tags and preserve the rest of the workflow.
- [x] 2.1 Run exact-reference and scope checks plus `git diff --check`.
- [x] 2.2 Confirm `sonar-project.properties` remains identity-only.

## Six-Line SHA-Only Implementation Evidence

| Workflow line(s) | Action | Full SHA |
|---|---|---|
| 47, 53 | `docker/metadata-action` | `dc802804100637a589fabce1cb79ff13a1411302` |
| 58 | `docker/login-action` | `dbcb813823bdd20940b903addbd779551569679f` |
| 64 | `docker/setup-buildx-action` | `37fe631027851001ddb9b187196cc803df7f5f0e` |
| 67, 77 | `docker/build-push-action` | `53b7df96c91f9c12dcc8a07bcb9ccacbed38856a` |

The six listed `uses:` occurrences are the complete narrowed implementation evidence. Existing checkout and Sonar action SHA values remain unchanged. The current workflow has no added runtime, coverage, LCOV, artifact-transfer, or validation steps. `sonar-project.properties` contains only its original active project identity properties, and `git diff --check` passes.

## Unrelated/Superseded Working-Tree Changes

The repository also contains broader implementation changes from an earlier superseded SDD attempt, including Dockerfile/runtime changes, Compose/Nginx changes, package and lockfile changes, frontend/backend tests and coverage tooling, and application-source edits. Those changes are pre-existing/outside this reconciliation, are not represented as SHA-only work, and must not be silently attributed to `sonar-quality-hardening`.

## Verification Boundary

No remote SonarCloud workflow run occurred. This reconciliation therefore makes no claim about a remote Quality Gate, an A rating, or any remote Sonar metric. `verify-report.md` was intentionally not updated; it must be regenerated by the dedicated `sdd-verify` phase.
